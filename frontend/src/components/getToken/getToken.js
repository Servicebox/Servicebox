{/*const clientId = '51768409';
const scope = 'offline,messages';

const getToken = () => {
  const redirectUri = 'https://oauth.vk.com/blank.html';
  const responseType = 'token';
  const version = '5.37';

  const url = `https://oauth.vk.com/authorize?client_id=${clientId}&display=page&redirect_uri=${redirectUri}&scope=${scope}&response_type=${responseType}&v=${version}`;
  
  window.location.href = url;
}

export default getToken;*/}