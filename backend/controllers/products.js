//controllers/products/js
{/*const checkAdmin = (req, res, next) => {
  if (req.user && req.user.role === 'admin') {
    next(); // Проход дальше, пользователь - администратор
  } else {
    res.status(403).json({ message: 'Forbidden' }); // Ошибка доступа
  }
};*/}